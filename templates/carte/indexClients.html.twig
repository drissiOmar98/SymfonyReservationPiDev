
{% extends 'reservation/shop.html.twig' %}





{% block section1 %}

<div class="container mb-4">
    <div class="row">
        <div class="col-12">
            {% if items | length > 0 %}
            <div class="table-responsive">
                <table class="table table-striped">



                    <thead>
                    <tr>
                        <th scope="col"> </th>
                        <th scope="col">Offres</th>
                        <th scope="col">Available</th>
                        <th scope="col" class="text-center">Quantity</th>
                        <th scope="col" class="text-right">Price</th>
                    </tr>
                    </thead>

                    <tbody>
                    {% for item in items %}

                    </thead>
                    <tbody>
                    <tr>
                        <td><img src="{{ asset('assets/media/photos/photo1.jpg')  }}" class="fixedimagepan" width="150"/> </td>

                        <td>{{ item.prod.path }}</td>
                        <td>Disponible</td>

                        <td><p class="form-control"/>{{ item.quantite}}</p></td>
                        <td class="text-right">{{ item.prod.prix * item.quantite}} dt</td>

                        <td>
                        <td class="font-w600 font-size-sm"><a href="{{ path('reservaide_index', {'id': item.prod.id}) }}">  <div class="button mt-2 d-flex flex-row align-items-center"> <button class="btn btn-sm btn-outline-primary w-100">My Reservation</button> </div></a></td>
                        </td>
                    </tr>

                    {% endfor %}
                    <tr>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td>Sub-Total</td>
                        <td class="text-right">{{ total }} dt</td>
                    </tr>
                    <tr>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td>Tax (12%)</td>
                        <td class="text-right">{{(total * 12)/ 100 }}</td>
                    </tr>
                    <tr>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td><strong>Total</strong></td>
                        <td class="text-right"><strong>{{ total * 1.12 }}</strong></td>
                    </tr>
                    </tbody>
                </table>
            </div>
        </div>
        <div class="col mb-2">
            <div class="row">
                <div class="col-sm-12  col-md-6">
                    <a href="{{path('AffiHotelback')}}"class="btn btn-block btn-light">Continue Shopping</a>
                </div>

            </div>
        </div>


        {% else %}



                <div>
                    <h1>Surprise! c'est vide
                    </h1>
                </div>




        {% endif %}

        </div>
    </div>



</div>
        {% endblock %}

